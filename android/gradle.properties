org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
# Kotlin's incremental compiler stores source paths relative to the project and
# crashes when a dependency lives on another Windows drive — this project is on
# E: while the pub cache is on C:. Full compiles are slower but do not throw
# "this and base files have different roots".
kotlin.incremental=false
